services:
  - type: web
    name: biblioteca-virtual
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_DEBUG
        value: "0"
      - key: DATABASE_PATH
        value: "biblioteca.db"
